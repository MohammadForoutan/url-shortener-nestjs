<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Verify Your Email</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f7f7f7;
        padding: 32px;
        color: #222;
      }
      .container {
        max-width: 480px;
        background: #fff;
        margin: 0 auto;
        padding: 32px 24px;
        border-radius: 8px;
        box-shadow: 0 6px 24px rgba(0, 0, 0, 0.07);
        text-align: center;
      }
      .verify-button {
        background-color: #3366ff;
        color: #fff;
        padding: 14px 28px;
        border: none;
        border-radius: 4px;
        font-size: 1.1em;
        cursor: pointer;
        margin-top: 18px;
        text-decoration: none;
        display: inline-block;
      }
      .small {
        font-size: 0.93em;
        color: #555;
        margin-top: 32px;
        display: block;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Email Verification</h2>
      <p>
        Hi,<br />
        Thank you for registering. Please verify your email address by clicking
        the button below.
      </p>
      <a class="verify-button" href="<%= verificationLink %>" target="_blank">
        Verify Email
      </a>
      <p class="small">
        If you did not request this, you can safely ignore this message.<br />
        This link will expire in 24 hours.
      </p>
    </div>
  </body>
</html>
